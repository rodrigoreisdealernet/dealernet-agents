# ESO SecretStore for dia-dev → dev-grade OpenBao (ADR-0100).
# Bootstrapped by a cluster admin (the namespace-scoped gha-deployer does not manage it).
# Apply with: kubectl apply -f deploy/k8s/dia-dev/secretstore-openbao.yaml
#
# Prereqs: External Secrets Operator installed cluster-wide, and OpenBao bootstrapped
# per deploy/k8s/dia-vault/openbao-dev.yaml (Kubernetes auth role `dia-eso` bound to
# the eso-vault-auth ServiceAccount below).
---
# ServiceAccount ESO authenticates AS to OpenBao (bound to the dia-eso role).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-vault-auth
  namespace: dia-dev
  labels:
    app.kubernetes.io/part-of: dia
---
# Namespaced SecretStore (fits the namespace-scoped gha-deployer). ESO vault provider
# talks to dev-grade OpenBao over HTTP (no TLS in dev). API: external-secrets.io/v1
# (ESO v2.x no longer serves v1beta1).
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: openbao-dev
  namespace: dia-dev
  labels:
    app.kubernetes.io/part-of: dia
spec:
  provider:
    vault:
      server: "http://openbao.dia-vault.svc:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "dia-eso"
          serviceAccountRef:
            name: "eso-vault-auth"
